{
  "name": "cf-flag",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "make build",
    "deploy": "make deploy",
    "dev": "make dev",
    "start": "make dev",
    "db:init": "make db-init",
    "db:init:remote": "make db-init-remote",
    "test": "make test"
  },
  "devDependencies": {
    "wrangler": "^4.84.1"
  }
}
